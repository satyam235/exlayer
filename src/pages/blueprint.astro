---
import Layout from '../layouts/Layout.astro';
---
<Layout title="Exlayer — The AI Remediation Execution Layer" themeColor="#07071a" fontHref={"https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600&family=Space+Grotesk:wght@400;500;600;700&display=swap"}>
<style is:inline slot="head">
:root{
  --bg:#07071a; --panel:#0c0c1e; --panel-2:#11112a; --line:rgba(255,255,255,.08); --line-2:rgba(255,255,255,.14);
  --cyan:#7b8cff; --cyan-br:#9ba8ff; --teal:#22d3ee; --ind:#7b8cff; --grid:rgba(123,140,255,.07);
  --em:#3dd68c; --amber:#f5a623; --rose:#ff3268;
  --t1:#eef0f8; --t2:#a7adc6; --t3:#5c6080;
  --mono:'IBM Plex Mono',monospace; --disp:'Space Grotesk',sans-serif;
}
*{margin:0;padding:0;box-sizing:border-box;}
html{scroll-behavior:smooth;}
body{font-family:var(--disp);color:var(--t1);overflow-x:hidden;line-height:1.55;
  background:radial-gradient(ellipse 90% 50% at 15% -5%,rgba(123,140,255,.13),transparent 55%),
    radial-gradient(ellipse 70% 50% at 95% 8%,rgba(255,50,104,.05),transparent 55%),var(--bg);}
::selection{background:rgba(123,140,255,.25);}
a{color:inherit;text-decoration:none;}
.wrap{max-width:1180px;margin:0 auto;padding:0 32px;}
.mono{font-family:var(--mono);}
.grid-bg{position:fixed;inset:0;z-index:0;pointer-events:none;
  background-image:linear-gradient(var(--grid) 1px,transparent 1px),linear-gradient(90deg,var(--grid) 1px,transparent 1px);
  background-size:40px 40px;-webkit-mask-image:radial-gradient(ellipse 100% 80% at 50% 0%,#000 40%,transparent 85%);
  mask-image:radial-gradient(ellipse 100% 80% at 50% 0%,#000 40%,transparent 85%);}

nav{position:fixed;top:0;left:0;right:0;z-index:50;transition:background .3s,border-color .3s;border-bottom:1px solid transparent;}
nav.scrolled{background:rgba(7,7,26,.78);backdrop-filter:blur(14px);border-bottom-color:var(--line);}
.nav-in{display:flex;align-items:center;justify-content:space-between;height:68px;}
.logo{display:flex;align-items:center;gap:10px;font-weight:700;font-size:18px;letter-spacing:-.02em;}
.logo-mark{width:30px;height:30px;border-radius:7px;border:1.5px solid var(--cyan);position:relative;box-shadow:0 0 18px rgba(123,140,255,.4);display:flex;align-items:center;justify-content:center;}
.logo-mark::after{content:'';width:12px;height:12px;border:1.5px solid var(--cyan);border-radius:2px;}
.nav-links{display:flex;align-items:center;gap:32px;}
.nav-links a:not(.btn){font-family:var(--mono);font-size:13px;color:var(--t2);transition:color .2s;}
.nav-links a:not(.btn):hover{color:var(--cyan-br);}
@media(max-width:820px){.nav-links a:not(.btn){display:none;}}
.btn{display:inline-flex;align-items:center;gap:8px;font-family:var(--mono);font-weight:500;font-size:13px;cursor:pointer;border:none;transition:transform .15s cubic-bezier(.16,1,.3,1),box-shadow .2s,background .2s;}
.btn-pri{background:var(--cyan);color:#08071c;padding:11px 20px;border-radius:8px;box-shadow:0 0 24px -4px rgba(123,140,255,.55);font-weight:600;}
.btn-pri:hover{transform:translateY(-2px);background:var(--cyan-br);box-shadow:0 0 34px -2px rgba(123,140,255,.7);}
.btn-ghost{background:transparent;color:var(--cyan-br);padding:11px 20px;border-radius:8px;border:1px solid var(--line-2);}
.btn-ghost:hover{background:rgba(123,140,255,.08);transform:translateY(-2px);}

/* hero */
.hero{position:relative;z-index:1;padding:140px 0 70px;text-align:center;}
.eyebrow{display:inline-flex;align-items:center;gap:9px;font-family:var(--mono);font-size:11.5px;letter-spacing:.2em;text-transform:uppercase;color:var(--cyan-br);border:1px solid var(--line-2);padding:7px 14px;border-radius:6px;margin-bottom:26px;}
.eyebrow .pulse{width:6px;height:6px;border-radius:50%;background:var(--em);box-shadow:0 0 0 0 rgba(61,214,140,.5);animation:ping 2.2s infinite;}
@keyframes ping{0%{box-shadow:0 0 0 0 rgba(61,214,140,.5);}70%{box-shadow:0 0 0 8px rgba(61,214,140,0);}100%{box-shadow:0 0 0 0 rgba(61,214,140,0);}}
h1{font-family:var(--disp);font-size:clamp(40px,5.8vw,72px);font-weight:800;letter-spacing:-.035em;line-height:1.03;margin-bottom:22px;}
h1 em{font-style:normal;color:var(--cyan);text-shadow:0 0 40px rgba(123,140,255,.5);}
h1 .nowrap{white-space:nowrap;}
.sub{font-size:18px;color:var(--t2);line-height:1.6;max-width:620px;margin:0 auto 32px;}
.hero-cta{display:flex;gap:14px;flex-wrap:wrap;align-items:center;justify-content:center;}

/* schematic */
.schematic{margin-top:60px;position:relative;z-index:1;}
.canvas{border:1px solid var(--line-2);border-radius:14px;background:linear-gradient(180deg,var(--panel),rgba(7,7,26,.6));padding:20px;position:relative;overflow:hidden;box-shadow:0 40px 90px -40px rgba(0,0,0,.9);}
.canvas .corner{position:absolute;width:14px;height:14px;border-color:var(--cyan);opacity:.6;}
.canvas .corner.tl{top:8px;left:8px;border-top:1.5px solid;border-left:1.5px solid;}
.canvas .corner.tr{top:8px;right:8px;border-top:1.5px solid;border-right:1.5px solid;}
.canvas .corner.bl{bottom:8px;left:8px;border-bottom:1.5px solid;border-left:1.5px solid;}
.canvas .corner.br{bottom:8px;right:8px;border-bottom:1.5px solid;border-right:1.5px solid;}
.canvas-lbl{position:absolute;top:14px;left:50%;transform:translateX(-50%);font-family:var(--mono);font-size:10px;letter-spacing:.2em;color:var(--t3);text-transform:uppercase;}
.schem-svg{width:100%;height:auto;display:block;}
.flowpath{fill:none;stroke:var(--line-2);stroke-width:1.5;stroke-dasharray:600;stroke-dashoffset:600;animation:draw 1.4s cubic-bezier(.16,1,.3,1) forwards;}
@keyframes draw{to{stroke-dashoffset:0;}}
.node-rect{fill:var(--panel-2);stroke:var(--line-2);stroke-width:1.2;}
.node-rect.engine{fill:rgba(123,140,255,.08);stroke:var(--cyan);}
.node-text{font-family:'IBM Plex Mono',monospace;font-size:11px;fill:var(--t1);}
.node-sub{font-family:'IBM Plex Mono',monospace;font-size:8.5px;fill:var(--t3);letter-spacing:.08em;}
.src-text{font-family:'IBM Plex Mono',monospace;font-size:10px;fill:var(--t1);}
.node-cap{font-family:'IBM Plex Mono',monospace;font-size:8px;fill:var(--t2);letter-spacing:.02em;}
.flowpath.ret{stroke:rgba(61,214,140,.55);stroke-width:1.8;stroke-dasharray:7 6;stroke-dashoffset:0;animation:dash 22s linear infinite;}
@keyframes dash{to{stroke-dashoffset:-260;}}
.node-rect.exec{stroke:rgba(123,140,255,.32);}
.exec-text{font-family:var(--mono);font-size:9.5px;fill:var(--t1);}
.vtitle{font-family:var(--disp);font-size:13px;font-weight:700;fill:var(--t2);transition:fill .5s;}
.chk circle{fill:var(--em);}
.chk path{fill:none;stroke:#06231a;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;}
.exec-node .ebox,.exec-node .etext{transition:stroke .45s cubic-bezier(.16,1,.3,1),fill .45s cubic-bezier(.16,1,.3,1);}
.exec-node.done .ebox{stroke:rgba(61,214,140,.6);fill:rgba(61,214,140,.07);}
.exec-node.done .etext{fill:var(--em);}
.ebadge,.vbadge{opacity:0;transform:scale(.4);transform-box:fill-box;transform-origin:center;transition:opacity .4s cubic-bezier(.16,1,.3,1),transform .4s cubic-bezier(.16,1,.3,1);}
.exec-node.done .ebadge{opacity:1;transform:scale(1);}
.vgate .vbox{transition:stroke .5s,fill .5s;}
.vgate.done .vbox{stroke:rgba(61,214,140,.7);fill:rgba(61,214,140,.08);}
.vgate.done .vtitle{fill:var(--em);}
.vgate.done .vbadge{opacity:1;transform:scale(1);}
.vstamp{opacity:0;transition:opacity .5s .1s;}
.vgate.done .vstamp{opacity:1;}
.mem .mbox{transition:stroke .5s,fill .5s;}
.mem.pulse .mbox{stroke:rgba(123,140,255,.55);fill:rgba(123,140,255,.10);}
.mem-cap{font-family:var(--mono);font-size:8.5px;fill:var(--t2);letter-spacing:.02em;}
.fb-lbl{font-family:var(--mono);font-size:8.5px;letter-spacing:.04em;}
.schem-legend{display:flex;gap:22px;justify-content:center;flex-wrap:wrap;margin-top:18px;font-family:var(--mono);font-size:11px;color:var(--t2);}
.schem-legend i{display:inline-block;width:10px;height:10px;border-radius:3px;margin-right:7px;vertical-align:-1px;}
.engine-title{font-family:'Space Grotesk',sans-serif;font-size:13px;font-weight:600;fill:var(--cyan-br);}
.packet{fill:var(--cyan);filter:drop-shadow(0 0 4px var(--cyan));}
.packet.em{fill:var(--em);filter:drop-shadow(0 0 4px var(--em));}
.col-lbl{font-family:'IBM Plex Mono',monospace;font-size:9px;letter-spacing:.18em;fill:var(--t3);}

/* reveal */
.reveal{opacity:0;transform:translateY(26px);transition:opacity .7s cubic-bezier(.16,1,.3,1),transform .7s cubic-bezier(.16,1,.3,1);}
.reveal.in{opacity:1;transform:none;}
.reveal.d1{transition-delay:.08s;}.reveal.d2{transition-delay:.16s;}.reveal.d3{transition-delay:.24s;}.reveal.d4{transition-delay:.32s;}

section{position:relative;z-index:1;}
.sec-head{max-width:680px;margin:0 auto 50px;text-align:center;}
.sec-eyebrow{font-family:var(--mono);font-size:11px;letter-spacing:.2em;text-transform:uppercase;color:var(--cyan-br);margin-bottom:14px;}
.sec-eyebrow::before{content:'// ';color:var(--t3);}
.sec-title{font-family:var(--disp);font-size:clamp(28px,3.8vw,44px);font-weight:800;letter-spacing:-.03em;line-height:1.1;margin-bottom:16px;}
.sec-sub{color:var(--t2);font-size:16px;line-height:1.6;}

/* trust */
.trust{padding:50px 0;border-top:1px solid var(--line);border-bottom:1px solid var(--line);}
.trust-lbl{text-align:center;font-family:var(--mono);font-size:11px;letter-spacing:.16em;text-transform:uppercase;color:var(--t3);margin-bottom:26px;}
.logos{display:flex;flex-wrap:wrap;justify-content:center;gap:12px;}
.logo-pill{display:inline-flex;align-items:center;gap:9px;border:1px solid var(--line);border-radius:8px;padding:10px 15px;background:var(--panel);transition:border-color .25s,transform .25s;}
.logo-pill:hover{border-color:var(--line-2);transform:translateY(-3px);}
.logo-pill .lm{width:24px;height:24px;border-radius:5px;display:flex;align-items:center;justify-content:center;font-family:var(--mono);font-weight:600;font-size:11px;color:#08071c;}
.logo-pill span{font-family:var(--mono);font-size:12.5px;color:var(--t2);white-space:nowrap;}

/* why-now section */
.why{padding:90px 0;}
.why-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;}
@media(max-width:900px){.why-grid{grid-template-columns:1fr;}}
.why-card{position:relative;border:1px solid var(--line);border-radius:16px;padding:28px 26px 26px;
  background:linear-gradient(180deg,var(--panel),rgba(7,7,26,.35));overflow:hidden;
  transition:border-color .3s,transform .3s,box-shadow .3s;}
.why-card::before{content:'';position:absolute;inset:0;border-radius:16px;pointer-events:none;opacity:0;transition:opacity .3s;
  background:radial-gradient(130% 90% at 0% 0%,rgba(123,140,255,.10),transparent 55%);}
.why-card:hover{transform:translateY(-6px);border-color:var(--line-2);box-shadow:0 30px 60px -34px rgba(123,140,255,.45);}
.why-card:hover::before{opacity:1;}
.why-card.gap{border-color:rgba(123,140,255,.32);}
.why-card.gap::after{content:'';position:absolute;left:0;top:0;width:100%;height:2px;background:linear-gradient(90deg,var(--cyan),transparent);}
.why-ico{height:60px;margin-bottom:22px;}
.why-ico svg{width:100%;height:100%;overflow:visible;}
.why-tag{font-family:var(--mono);font-size:10.5px;letter-spacing:.18em;text-transform:uppercase;color:var(--cyan-br);margin-bottom:12px;}
.why-tag::before{content:'// ';color:var(--t3);}
.why-card h3{font-family:var(--disp);font-size:20px;font-weight:700;letter-spacing:-.02em;line-height:1.2;margin-bottom:10px;}
.why-card p{color:var(--t2);font-size:14px;line-height:1.6;}
.glyph{fill:none;stroke-width:2;stroke-linecap:round;stroke-linejoin:round;}
.glyph.line{stroke:var(--cyan);}
.glyph.dim{stroke:var(--t3);}
.glyph.warn{stroke:var(--amber);}
.glyph.thick{stroke-width:4;}
.glyph-dot{fill:var(--cyan);}
.why-ico .draw{stroke-dasharray:340;stroke-dashoffset:340;transition:stroke-dashoffset 1.15s cubic-bezier(.16,1,.3,1) .15s;}
.why-card.in .draw{stroke-dashoffset:0;}

/* steps */
.steps-wrap{padding:90px 0;}
.steps{display:grid;grid-template-columns:repeat(4,1fr);gap:16px;}
@media(max-width:980px){.steps{grid-template-columns:repeat(2,1fr);}}
@media(max-width:560px){.steps{grid-template-columns:1fr;}}
.step{border:1px solid var(--line);border-radius:12px;padding:26px 22px;background:var(--panel);transition:border-color .3s,transform .3s,box-shadow .3s;position:relative;}
.step::before{content:'';position:absolute;top:0;left:22px;width:30px;height:2px;background:var(--cyan);box-shadow:0 0 10px var(--cyan);}
.step:hover{border-color:var(--line-2);transform:translateY(-5px);box-shadow:0 24px 50px -30px rgba(123,140,255,.5);}
.step .n{font-family:var(--mono);font-size:11px;color:var(--cyan-br);margin:14px 0 16px;}
.step .ic{width:44px;height:44px;border-radius:10px;border:1px solid var(--line-2);display:flex;align-items:center;justify-content:center;margin-bottom:16px;color:var(--cyan);}
.step h3{font-family:var(--disp);font-size:18px;font-weight:600;margin-bottom:9px;}
.step p{color:var(--t2);font-size:13px;line-height:1.55;}

/* engine */
.engine{padding:90px 0;}
.eng-grid{display:grid;grid-template-columns:1fr 1fr;gap:54px;align-items:center;}
@media(max-width:920px){.eng-grid{grid-template-columns:1fr;gap:40px;}}
.eng-list{margin-top:24px;display:flex;flex-direction:column;gap:6px;}
.eng-item{display:flex;gap:14px;padding:14px;border:1px solid transparent;border-radius:10px;transition:border-color .25s,background .25s;}
.eng-item:hover{border-color:var(--line);background:var(--panel);}
.eng-item .ck{width:24px;height:24px;border-radius:6px;border:1px solid var(--line-2);display:flex;align-items:center;justify-content:center;color:var(--em);flex-shrink:0;}
.eng-item h4{font-family:var(--disp);font-size:15.5px;font-weight:600;margin-bottom:3px;}
.eng-item p{color:var(--t2);font-size:13.5px;line-height:1.55;}
.term{border:1px solid var(--line-2);border-radius:12px;overflow:hidden;background:rgba(7,7,26,.85);box-shadow:0 30px 70px -34px rgba(0,0,0,.9);}
.term-bar{display:flex;align-items:center;gap:7px;padding:12px 15px;border-bottom:1px solid var(--line);}
.term-bar .d{width:9px;height:9px;border-radius:50%;border:1px solid var(--line-2);}
.term-bar .nm{margin-left:8px;font-family:var(--mono);font-size:10.5px;color:var(--t3);letter-spacing:.08em;}
.term-body{padding:16px 16px;font-family:var(--mono);font-size:12px;line-height:1.9;min-height:180px;}
.tl{display:flex;gap:10px;opacity:0;transform:translateY(4px);}
.tl.show{opacity:1;transform:none;transition:all .3s;}
.tl .tm{color:var(--t3);flex-shrink:0;}.tl .st{color:var(--cyan-br);width:78px;flex-shrink:0;}.tl .ms{color:var(--t2);}
.tk{padding:1px 5px;border-radius:3px;}.tk.c{color:var(--cyan-br);background:rgba(123,140,255,.12);}.tk.k{color:var(--amber);background:rgba(245,166,35,.12);}.tk.o{color:var(--em);background:rgba(61,214,140,.12);}
.caret{display:inline-block;width:7px;height:13px;background:var(--cyan);vertical-align:-2px;animation:blink 1s steps(1) infinite;}
@keyframes blink{50%{opacity:0;}}

/* outcomes */
.outcomes{padding:90px 0;}
.out-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:16px;}
@media(max-width:820px){.out-grid{grid-template-columns:repeat(2,1fr);}}
.out{border:1px solid var(--line);border-radius:12px;padding:28px 22px;background:var(--panel);}
.out .num{font-family:var(--disp);font-size:clamp(36px,4.5vw,52px);font-weight:800;letter-spacing:-.03em;line-height:1;color:var(--cyan);font-feature-settings:"tnum";}
.out .lbl{color:var(--t2);font-size:13px;margin-top:10px;font-family:var(--mono);}

/* security */
.sec-strip{padding:90px 0;}
.badges{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin-top:14px;}
@media(max-width:820px){.badges{grid-template-columns:1fr 1fr;}}
.badge{border:1px solid var(--line);border-radius:12px;padding:22px 20px;background:var(--panel);transition:transform .3s,border-color .3s;}
.badge:hover{transform:translateY(-4px);border-color:var(--line-2);}
.badge .ic{width:38px;height:38px;border-radius:9px;border:1px solid var(--line-2);display:flex;align-items:center;justify-content:center;color:var(--cyan);margin-bottom:14px;}
.badge b{font-family:var(--disp);font-size:14.5px;font-weight:600;display:block;margin-bottom:3px;}
.badge span{font-family:var(--mono);font-size:11px;color:var(--t3);}

/* cta */
.cta{padding:110px 0;text-align:center;}
.cta-inner{border:1px solid var(--line-2);border-radius:20px;padding:70px 40px;position:relative;overflow:hidden;
  background:radial-gradient(ellipse 70% 130% at 50% 0%,rgba(123,140,255,.14),transparent 60%),var(--panel);}
.cta-inner .corner{position:absolute;width:16px;height:16px;border-color:var(--cyan);opacity:.6;}
.cta-inner .tl{top:12px;left:12px;border-top:1.5px solid;border-left:1.5px solid;}
.cta-inner .tr{top:12px;right:12px;border-top:1.5px solid;border-right:1.5px solid;}
.cta-inner .bl{bottom:12px;left:12px;border-bottom:1.5px solid;border-left:1.5px solid;}
.cta-inner .br{bottom:12px;right:12px;border-bottom:1.5px solid;border-right:1.5px solid;}
.cta h2{font-family:var(--disp);font-size:clamp(30px,4.5vw,50px);font-weight:800;letter-spacing:-.03em;margin-bottom:16px;}
.cta p{color:var(--t2);font-size:17px;margin-bottom:32px;}
.cta .hero-cta{justify-content:center;}

footer{border-top:1px solid var(--line);padding:50px 0 38px;position:relative;z-index:1;}
.foot-grid{display:flex;justify-content:space-between;gap:40px;flex-wrap:wrap;margin-bottom:34px;}
.foot-col h5{font-family:var(--mono);font-size:10.5px;letter-spacing:.14em;text-transform:uppercase;color:var(--t3);margin-bottom:16px;}
.foot-col a{display:block;color:var(--t2);font-size:13.5px;margin-bottom:10px;font-family:var(--mono);transition:color .2s;}
.foot-col a:hover{color:var(--cyan-br);}
.foot-bot{display:flex;justify-content:space-between;align-items:center;padding-top:24px;border-top:1px solid var(--line);font-family:var(--mono);font-size:12px;color:var(--t3);flex-wrap:wrap;gap:12px;}
</style>
<div class="grid-bg"></div>

<nav id="nav">
  <div class="wrap nav-in">
    <div class="logo"><span class="logo-mark"></span> Exlayer</div>
    <div class="nav-links">
      <a href="#why">Why Exlayer</a><a href="#how">How it works</a><a href="#faq">FAQ</a>
      <a href="#demo" class="btn btn-pri">Book a demo</a>
    </div>
  </div>
</nav>

<!-- HERO -->
<header class="hero">
  <div class="wrap">
    <div class="eyebrow reveal"><span class="pulse"></span> Machine-speed security remediation</div>
    <h1 class="reveal d1">The execution layer <span class="nowrap">for <em>security.</em></span></h1>
    <p class="sub reveal d2">Exlayer is the execution layer between detection and closure. It understands the finding, selects the safest remediation, applies policy and approvals, executes the change and validates that the exposure is closed.</p>
    <div class="hero-cta reveal d3">
      <a href="#demo" class="btn btn-pri">Book a demo →</a>
    </div>
    <!-- SCHEMATIC -->
    <div class="schematic reveal d2">
      <div class="canvas">
        <span class="corner tl"></span><span class="corner tr"></span><span class="corner bl"></span><span class="corner br"></span>
        <span class="canvas-lbl">EXLAYER · FINDING → VERIFIED CLOSURE</span>
        <svg class="schem-svg" viewBox="0 0 1120 500" preserveAspectRatio="xMidYMid meet">
          <defs>
            <marker id="ahg" markerWidth="9" markerHeight="9" refX="6" refY="3" orient="auto"><path d="M0,0 L6,3 L0,6 Z" fill="rgba(61,214,140,.9)"/></marker>
            <marker id="ahb" markerWidth="9" markerHeight="9" refX="6" refY="3" orient="auto"><path d="M0,0 L6,3 L0,6 Z" fill="var(--cyan-br)"/></marker>
          </defs>

          <!-- column labels -->
          <text class="col-lbl" x="40" y="48">FINDING SOURCES</text>
          <text class="col-lbl" x="660" y="48">EXECUTION METHODS</text>
          <text class="col-lbl" x="905" y="48">VALIDATION</text>

          <!-- findings -> engine -->
          <path class="flowpath" d="M250,82 C312,82 326,220 360,220" style="animation-delay:.10s"/>
          <path class="flowpath" d="M250,142 C312,142 340,220 360,220" style="animation-delay:.18s"/>
          <path class="flowpath" d="M250,202 C320,202 350,220 360,220" style="animation-delay:.26s"/>
          <path class="flowpath" d="M250,262 C320,262 350,220 360,220" style="animation-delay:.34s"/>
          <path class="flowpath" d="M250,322 C312,322 340,220 360,220" style="animation-delay:.42s"/>
          <path class="flowpath" d="M250,382 C312,382 326,220 360,220" style="animation-delay:.50s"/>
          <!-- intel -> engine -->
          <path class="flowpath" d="M477,94 L477,110" style="animation-delay:.55s"/>
          <!-- engine -> execution -->
          <path class="flowpath" d="M595,220 C632,220 624,100 660,100" style="animation-delay:.7s"/>
          <path class="flowpath" d="M595,220 C632,220 628,160 660,160" style="animation-delay:.76s"/>
          <path class="flowpath" d="M595,220 L660,220" style="animation-delay:.82s"/>
          <path class="flowpath" d="M595,220 C632,220 628,280 660,280" style="animation-delay:.88s"/>
          <path class="flowpath" d="M595,220 C632,220 624,340 660,340" style="animation-delay:.94s"/>
          <!-- execution -> validation -->
          <path class="flowpath" d="M875,100 C892,100 898,220 905,220" style="animation-delay:1.05s"/>
          <path class="flowpath" d="M875,160 C892,160 902,220 905,220" style="animation-delay:1.10s"/>
          <path class="flowpath" d="M875,220 L905,220" style="animation-delay:1.15s"/>
          <path class="flowpath" d="M875,280 C892,280 902,220 905,220" style="animation-delay:1.20s"/>
          <path class="flowpath" d="M875,340 C892,340 898,220 905,220" style="animation-delay:1.25s"/>

          <!-- feedback: validation -> memory -> engine -->
          <path class="flowpath ret" marker-end="url(#ahg)" d="M1000,320 C1000,392 1000,443 950,443 L879,443"/>
          <text class="fb-lbl" x="992" y="372" text-anchor="end" fill="rgba(61,214,140,.9)">verified outcome</text>
          <path class="flowpath" marker-end="url(#ahb)" d="M470,408 L470,332"/>
          <text class="fb-lbl" x="482" y="374" fill="var(--cyan-br)">informs the next fix</text>

          <!-- finding sources -->
          <g><rect class="node-rect" x="40" y="60" width="210" height="44" rx="8"/><text class="src-text" x="54" y="87">Vulnerability scanners</text></g>
          <g><rect class="node-rect" x="40" y="120" width="210" height="44" rx="8"/><text class="src-text" x="54" y="147">EDR / XDR</text></g>
          <g><rect class="node-rect" x="40" y="180" width="210" height="44" rx="8"/><text class="src-text" x="54" y="207">Cloud security</text></g>
          <g><rect class="node-rect" x="40" y="240" width="210" height="44" rx="8"/><text class="src-text" x="54" y="267">Code and application security</text></g>
          <g><rect class="node-rect" x="40" y="300" width="210" height="44" rx="8"/><text class="src-text" x="54" y="327">Compliance and configuration tools</text></g>
          <g><rect class="node-rect" x="40" y="360" width="210" height="44" rx="8"/><text class="src-text" x="54" y="387">AI security agents</text></g>

          <!-- threat & exploit intel -->
          <g><rect class="node-rect" x="360" y="64" width="235" height="30" rx="7"/><text class="node-text" x="378" y="83">THREAT · EXPLOIT INTELLIGENCE</text></g>

          <!-- engine -->
          <rect class="node-rect engine" x="360" y="110" width="235" height="220" rx="14"/>
          <text class="engine-title" x="384" y="142">EXLAYER ENGINE</text>
          <text class="node-cap" x="384" y="168">Correlate findings</text>
          <text class="node-cap" x="384" y="188">Understand asset and business context</text>
          <text class="node-cap" x="384" y="208">Select the safest fix</text>
          <text class="node-cap" x="384" y="228">Apply policies and approvals</text>
          <text class="node-cap" x="384" y="248">Plan pilots, rollout and rollback</text>
          <rect x="384" y="272" width="172" height="6" rx="3" fill="rgba(123,140,255,.15)"/>
          <rect x="384" y="272" width="108" height="6" rx="3" fill="var(--cyan)"/>

          <!-- execution methods (flip to executed one-by-one) -->
          <g class="exec-node"><rect class="ebox node-rect exec" x="660" y="77" width="215" height="46" rx="8"/><text class="etext exec-text" x="674" y="104">OS &amp; third-party patching</text><g class="ebadge chk"><circle cx="857" cy="100" r="8"/><path d="M853.5 100 l2.6 2.6 l4.4 -5.4"/></g></g>
          <g class="exec-node"><rect class="ebox node-rect exec" x="660" y="137" width="215" height="46" rx="8"/><text class="etext exec-text" x="674" y="164">Configuration changes</text><g class="ebadge chk"><circle cx="857" cy="160" r="8"/><path d="M853.5 160 l2.6 2.6 l4.4 -5.4"/></g></g>
          <g class="exec-node"><rect class="ebox node-rect exec" x="660" y="197" width="215" height="46" rx="8"/><text class="etext exec-text" x="674" y="224">Scripts</text><g class="ebadge chk"><circle cx="857" cy="220" r="8"/><path d="M853.5 220 l2.6 2.6 l4.4 -5.4"/></g></g>
          <g class="exec-node"><rect class="ebox node-rect exec" x="660" y="257" width="215" height="46" rx="8"/><text class="etext exec-text" x="674" y="284">Software removal / deployment</text><g class="ebadge chk"><circle cx="857" cy="280" r="8"/><path d="M853.5 280 l2.6 2.6 l4.4 -5.4"/></g></g>
          <g class="exec-node"><rect class="ebox node-rect exec" x="660" y="317" width="215" height="46" rx="8"/><text class="etext exec-text" x="674" y="344">Compensating controls</text><g class="ebadge chk"><circle cx="857" cy="340" r="8"/><path d="M853.5 340 l2.6 2.6 l4.4 -5.4"/></g></g>

          <!-- validation (turns verified once executions complete) -->
          <g class="vgate" id="vgate">
            <rect class="vbox node-rect" x="905" y="120" width="190" height="200" rx="12"/>
            <g class="vbadge chk"><circle cx="1075" cy="142" r="10"/><path d="M1070.5 142 l3.2 3.2 l5.3 -6.4"/></g>
            <text class="vtitle" x="925" y="152">VALIDATION</text>
            <text class="node-cap" x="925" y="182">Re-scan the affected assets</text>
            <text class="node-cap" x="925" y="206">Confirm the exposure is gone</text>
            <text class="node-cap" x="925" y="230">Close the finding at its source</text>
            <g class="vstamp"><text class="mem-cap" x="925" y="274" fill="var(--em)" style="font-size:9.5px">&#10003; Closure verified</text></g>
          </g>

          <!-- memory (the agent's learning store) -->
          <g class="mem" id="memNode">
            <rect class="mbox node-rect" x="360" y="408" width="515" height="66" rx="12"/>
            <text class="engine-title" x="380" y="437" style="font-size:12px">EXLAYER MEMORY</text>
            <text class="mem-cap" x="380" y="458">Every verified outcome is recorded — so future fixes build on what already worked.</text>
          </g>

          <!-- moving packets -->
          <circle class="packet" r="3.2"><animateMotion dur="2.6s" begin="1.0s" repeatCount="indefinite" path="M250,82 C312,82 326,220 360,220"/></circle>
          <circle class="packet" r="3.2"><animateMotion dur="2.6s" begin="1.4s" repeatCount="indefinite" path="M250,202 C320,202 350,220 360,220"/></circle>
          <circle class="packet" r="3.2"><animateMotion dur="2.6s" begin="1.8s" repeatCount="indefinite" path="M250,382 C312,382 326,220 360,220"/></circle>
          <circle class="packet" r="3.0"><animateMotion dur="2.0s" begin="1.4s" repeatCount="indefinite" path="M477,94 L477,110"/></circle>
          <circle class="packet" r="3.2"><animateMotion dur="2.4s" begin="2.3s" repeatCount="indefinite" path="M595,220 C632,220 624,100 660,100"/></circle>
          <circle class="packet" r="3.2"><animateMotion dur="2.4s" begin="2.7s" repeatCount="indefinite" path="M595,220 C632,220 624,340 660,340"/></circle>
          <circle class="packet em" r="3.4"><animateMotion dur="3.0s" begin="3.4s" repeatCount="indefinite" path="M1000,320 C1000,392 1000,443 950,443 L879,443"/></circle>
          <circle class="packet" r="3.2"><animateMotion dur="2.4s" begin="3.9s" repeatCount="indefinite" path="M470,408 L470,332"/></circle>
        </svg>
      </div>
      <div class="schem-legend reveal">
        <span><i style="background:var(--cyan)"></i>finding flow</span>
        <span><i style="background:var(--em)"></i>validated closure loop</span>
        <span><i style="background:rgba(61,214,140,.2);border:1px solid rgba(61,214,140,.55)"></i>validation stage</span>
      </div>
    </div>
  </div>
</header>

<!-- TRUST -->
<section class="trust" id="integrations">
  <div class="wrap">
    <div class="trust-lbl reveal">// connects to every scanner you already run</div>
    <div class="logos">
      <div class="logo-pill reveal"><span class="lm" style="background:linear-gradient(135deg,#9ba8ff,#7b8cff)">S</span><span>SecOps</span></div>
      <div class="logo-pill reveal"><span class="lm" style="background:linear-gradient(135deg,#60a5fa,#3b82f6)">T</span><span>Tenable</span></div>
      <div class="logo-pill reveal d1"><span class="lm" style="background:linear-gradient(135deg,#34d399,#10b981)">D</span><span>Defender VM</span></div>
      <div class="logo-pill reveal d1"><span class="lm" style="background:linear-gradient(135deg,#f87171,#ef4444)">Q</span><span>Qualys</span></div>
      <div class="logo-pill reveal d2"><span class="lm" style="background:linear-gradient(135deg,#fb923c,#f97316)">R</span><span>Rapid7</span></div>
      <div class="logo-pill reveal d2"><span class="lm" style="background:linear-gradient(135deg,#a78bfa,#8b5cf6)">C</span><span>CrowdStrike Falcon</span></div>
      <div class="logo-pill reveal d3"><span class="lm" style="background:linear-gradient(135deg,#fbbf24,#f59e0b)">P</span><span>Palo Alto Cortex</span></div>
      <div class="logo-pill reveal d3"><span class="lm" style="background:linear-gradient(135deg,#22d3ee,#06b6d4)">Z</span><span>Zscaler Risk360</span></div>
    </div>
  </div>
</section>

<!-- WHY NOW -->
<section class="why" id="why">
  <div class="wrap">
    <div class="sec-head reveal">
      <div class="sec-eyebrow">why now</div>
      <h2 class="sec-title">Detection has outrun remediation.</h2>
      <p class="sec-sub">Findings pile up faster than they close — and most tools stop right before the fix.</p>
    </div>
    <div class="why-grid">
      <article class="why-card reveal">
        <div class="why-ico">
          <svg viewBox="0 0 240 60" preserveAspectRatio="xMinYMid meet">
            <line class="glyph dim" x1="8" y1="52" x2="232" y2="52" style="opacity:.3"/>
            <path class="glyph line draw" d="M8,50 C70,48 118,40 158,26 C194,14 214,9 230,6"/>
            <path class="glyph line" d="M222,5 L231,5 L231,14"/>
            <circle class="glyph-dot" cx="8" cy="50" r="2.6"/>
            <circle class="glyph-dot" cx="120" cy="36" r="2.6"/>
            <circle class="glyph-dot" cx="231" cy="6" r="3"/>
          </svg>
        </div>
        <div class="why-tag">detection</div>
        <h3>Detection is accelerating</h3>
        <p>Security tools and AI systems are producing findings faster than teams can resolve them.</p>
      </article>

      <article class="why-card reveal d1">
        <div class="why-ico">
          <svg viewBox="0 0 240 60" preserveAspectRatio="xMinYMid meet">
            <rect x="6" y="12" width="48" height="11" rx="5.5" fill="rgba(92,96,128,.45)"/>
            <rect x="70" y="33" width="40" height="11" rx="5.5" fill="rgba(123,140,255,.5)"/>
            <rect x="120" y="8" width="42" height="11" rx="5.5" fill="rgba(92,96,128,.4)"/>
            <rect x="128" y="40" width="30" height="11" rx="5.5" fill="rgba(92,96,128,.4)"/>
            <rect x="176" y="24" width="52" height="11" rx="5.5" fill="rgba(123,140,255,.42)"/>
            <circle cx="62" cy="17" r="2" fill="rgba(92,96,128,.6)"/>
            <circle cx="170" cy="45" r="2" fill="rgba(92,96,128,.6)"/>
          </svg>
        </div>
        <div class="why-tag">remediation</div>
        <h3>Remediation remains fragmented</h3>
        <p>Fixes are still coordinated across tickets, scripts, patching tools, spreadsheets and manual approvals.</p>
      </article>

      <article class="why-card gap reveal d2">
        <div class="why-ico">
          <svg viewBox="0 0 240 60" preserveAspectRatio="xMinYMid meet">
            <line class="glyph dim" x1="8" y1="30" x2="232" y2="30" style="opacity:.25"/>
            <path class="glyph line thick draw" d="M8,30 L150,30"/>
            <circle class="glyph-dot" cx="150" cy="30" r="4"/>
            <line class="glyph warn" x1="164" y1="30" x2="198" y2="30" style="stroke-dasharray:2 6"/>
            <circle class="glyph warn" cx="220" cy="30" r="10"/>
          </svg>
        </div>
        <div class="why-tag">the gap</div>
        <h3>Security tools stop before closure</h3>
        <p>Most products identify, prioritize or recommend. They do not safely execute and verify the remediation.</p>
      </article>
    </div>
  </div>
</section>

<!-- STEPS -->
<section class="steps-wrap" id="how">
  <div class="wrap">
    <div class="sec-head reveal">
      <div class="sec-eyebrow">pipeline</div>
      <h2 class="sec-title">From found to fixed — autonomously.</h2>
      <p class="sec-sub">One governed pipeline turns raw scanner output into executed remediation.</p>
    </div>
    <div class="steps">
      <div class="step reveal"><div class="ic"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3v10"/><polyline points="8 9 12 13 16 9"/><path d="M4 17v2a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2v-2"/></svg></div><div class="n">[01] INGEST</div><h3>Ingest</h3><p>Connect every scanner and EDR into one normalized exposure graph.</p></div>
      <div class="step reveal d1"><div class="ic"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><circle cx="6" cy="6" r="3"/><circle cx="18" cy="18" r="3"/><path d="M9 6h6a3 3 0 0 1 3 3v6"/><path d="M6 9v6a3 3 0 0 0 3 3h6"/></svg></div><div class="n">[02] CORRELATE</div><h3>Correlate</h3><p>De-duplicate across sources, enrich with threat &amp; exploit intelligence.</p></div>
      <div class="step reveal d2"><div class="ic"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><line x1="4" y1="6" x2="20" y2="6"/><line x1="4" y1="12" x2="14" y2="12"/><line x1="4" y1="18" x2="9" y2="18"/></svg></div><div class="n">[03] PRIORITIZE</div><h3>Prioritize</h3><p>Rank by real, exploited risk in your environment — not raw CVSS.</p></div>
      <div class="step reveal d3"><div class="ic"><svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></div><div class="n">[04] REMEDIATE</div><h3>Remediate</h3><p>Auto-drafted campaigns with approvals, pilot rings &amp; rollout.</p></div>
    </div>
  </div>
</section>

<!-- ENGINE -->
<section class="engine" id="engine">
  <div class="wrap eng-grid">
    <div>
      <div class="sec-eyebrow reveal" style="text-align:left">the engine</div>
      <h2 class="sec-title reveal d1" style="text-align:left">An engine that reasons, then acts.</h2>
      <p class="sec-sub reveal d2" style="text-align:left">It resolves intent, targets the fleet, and assembles a campaign — then you approve, pilot, and roll out. Every action is logged.</p>
      <div class="eng-list">
        <div class="eng-item reveal d2"><span class="ck"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span><div><h4>Drafts the fix automatically</h4><p>Patch deployment, remediation scripts, or software uninstall — per finding.</p></div></div>
        <div class="eng-item reveal d3"><span class="ck"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span><div><h4>Approvals &amp; pilot rings</h4><p>Gate execution behind review, validate on a pilot ring, promote to fleet.</p></div></div>
        <div class="eng-item reveal d4"><span class="ck"><svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></span><div><h4>Schedule &amp; roll back</h4><p>Run on approval or in a maintenance window, with full audit and rollback.</p></div></div>
      </div>
    </div>
    <div class="reveal d2">
      <div class="term">
        <div class="term-bar"><span class="d"></span><span class="d"></span><span class="d"></span><span class="nm">remediation-engine ~ live</span></div>
        <div class="term-body" id="termBody"></div>
      </div>
    </div>
  </div>
</section>

<!-- OUTCOMES -->
<section class="outcomes">
  <div class="wrap">
    <div class="sec-head reveal"><div class="sec-eyebrow">outcomes</div><h2 class="sec-title">Remediate in hours, not weeks.</h2></div>
    <div class="out-grid">
      <div class="out reveal"><div class="num"><span class="cu" data-to="90">0</span>%</div><div class="lbl">MTTR reduction</div></div>
      <div class="out reveal d1"><div class="num"><span class="cu" data-to="3" data-suffix=".2">0</span>×</div><div class="lbl">findings de-duplicated</div></div>
      <div class="out reveal d2"><div class="num"><span class="cu" data-to="100">0</span>%</div><div class="lbl">exploit-first coverage</div></div>
      <div class="out reveal d3"><div class="num"><span class="cu" data-to="100">0</span>%</div><div class="lbl">auditable actions</div></div>
    </div>
  </div>
</section>

<!-- SECURITY -->
<section class="sec-strip" id="security">
  <div class="wrap">
    <div class="sec-head reveal"><div class="sec-eyebrow">enterprise-ready</div><h2 class="sec-title">Trusted with production.</h2><p class="sec-sub">Every change is reviewed, piloted, and reversible — SaaS or fully on-prem, inside your perimeter.</p></div>
    <div class="badges">
      <div class="badge reveal"><div class="ic"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2l8 4v6c0 5-3.5 8-8 10-4.5-2-8-5-8-10V6z"/><polyline points="9 12 11.3 14.3 15.5 9.5"/></svg></div><b>Approval gates</b><span>review before run</span></div>
      <div class="badge reveal d1"><div class="ic"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7"><circle cx="12" cy="12" r="9"/><circle cx="12" cy="12" r="5"/><circle cx="12" cy="12" r="1.5"/></svg></div><b>Pilot rings</b><span>staged rollout</span></div>
      <div class="badge reveal d2"><div class="ic"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg></div><b>RBAC + SSO</b><span>SAML / OIDC</span></div>
      <div class="badge reveal d3"><div class="ic"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="3" width="16" height="18" rx="2"/><line x1="8" y1="8" x2="16" y2="8"/><line x1="8" y1="12" x2="16" y2="12"/><line x1="8" y1="16" x2="13" y2="16"/></svg></div><b>Audit trail</b><span>every action</span></div>
    </div>
  </div>
</section>

<!-- CTA -->
<section class="cta" id="demo">
  <div class="wrap">
    <div class="cta-inner reveal">
      <span class="corner tl"></span><span class="corner tr"></span><span class="corner bl"></span><span class="corner br"></span>
      <h2>From scan to fixed — automatically.</h2>
      <p>See Exlayer turn your real scanner findings into executed fixes — in 30 minutes.</p>
      <div class="hero-cta"><a href="#" class="btn btn-pri">Book a demo →</a><a href="#" class="btn btn-ghost">Talk to sales</a></div>
    </div>
  </div>
</section>

<footer>
  <div class="wrap">
    <div class="foot-grid">
      <div class="foot-col" style="max-width:260px">
        <div class="logo" style="margin-bottom:14px"><span class="logo-mark"></span> Exlayer</div>
        <p style="color:var(--t3);font-size:13px;line-height:1.6;font-family:var(--mono)">The execution layer for security remediation — from finding to fixed.</p>
      </div>
      <div class="foot-col"><h5>Product</h5><a href="#">Platform</a><a href="#">Integrations</a><a href="#">Security</a><a href="#">Pricing</a></div>
      <div class="foot-col"><h5>Company</h5><a href="#">About</a><a href="#">Careers</a><a href="#">Blog</a><a href="#">Contact</a></div>
      <div class="foot-col"><h5>Resources</h5><a href="#">Docs</a><a href="#">Changelog</a><a href="#">Trust center</a><a href="#">Status</a></div>
    </div>
    <div class="foot-bot"><span>© 2026 Exlayer, Inc.</span><span>// ai remediation engine</span></div>
  </div>
</footer>

<script is:inline>
const nav=document.getElementById('nav');
addEventListener('scroll',()=>nav.classList.toggle('scrolled',scrollY>20));
const io=new IntersectionObserver((es)=>{es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target);}})},{threshold:.15});
document.querySelectorAll('.reveal').forEach(el=>io.observe(el));
function countUp(el){const to=parseFloat(el.dataset.to),suf=el.dataset.suffix||'',dur=1500,t0=performance.now();
  function tick(t){let p=Math.min((t-t0)/dur,1);p=1-Math.pow(1-p,3);let v=to*p;el.textContent=(suf?v.toFixed(1):Math.round(v));
    if(p<1)requestAnimationFrame(tick);else el.textContent=(suf?to.toFixed(1):to);}requestAnimationFrame(tick);}
const cuio=new IntersectionObserver((es)=>{es.forEach(e=>{if(e.isIntersecting){countUp(e.target);cuio.unobserve(e.target);}})},{threshold:.6});
document.querySelectorAll('.cu').forEach(el=>cuio.observe(el));
// engine terminal
const lines=[
  {t:'00:00.2',st:'RESOLVE',ms:'intent → <span class="tk c">PATCH_DEPLOYMENT</span>'},
  {t:'00:00.8',st:'CORRELATE',ms:'412 findings → <b style="color:var(--t1)">128</b> unique'},
  {t:'00:01.4',st:'TARGET',ms:'<b style="color:var(--t1)">17</b> assets · <span class="tk k">1 exploited</span>'},
  {t:'00:02.0',st:'PLAN',ms:'pilot ring 3 → full rollout'},
  {t:'00:02.6',st:'READY',ms:'<span class="tk o">draft ready</span> — review'},
];
const body=document.getElementById('termBody');let li=0,started=false;
function typeLine(){
  if(li>=lines.length){setTimeout(()=>{body.innerHTML='';li=0;typeLine();},2600);return;}
  const l=lines[li];const div=document.createElement('div');div.className='tl';
  div.innerHTML=`<span class="tm">${l.t}</span><span class="st">${l.st}</span><span class="ms">${l.ms}<span class="caret"></span></span>`;
  body.appendChild(div);requestAnimationFrame(()=>div.classList.add('show'));
  const cs=body.querySelectorAll('.caret');cs.forEach((c,i)=>{if(i<cs.length-1)c.remove();});
  li++;setTimeout(typeLine,li===lines.length?800:650);
}
const tio=new IntersectionObserver((es)=>{es.forEach(e=>{if(e.isIntersecting&&!started){started=true;setTimeout(typeLine,400);}})},{threshold:.4});
tio.observe(document.querySelector('.term'));

// schematic: executions complete one-by-one, then validation verifies, then memory records — and loops
(function(){
  const execs=Array.from(document.querySelectorAll('.exec-node'));
  const vg=document.getElementById('vgate');
  const mem=document.getElementById('memNode');
  if(!execs.length||!vg)return;
  if(matchMedia('(prefers-reduced-motion: reduce)').matches){
    execs.forEach(e=>e.classList.add('done'));vg.classList.add('done');mem&&mem.classList.add('pulse');return;
  }
  function reset(){
    execs.forEach(e=>e.classList.remove('done'));vg.classList.remove('done');mem&&mem.classList.remove('pulse');
  }
  function run(){
    reset();
    execs.forEach((e,i)=>setTimeout(()=>e.classList.add('done'),700+i*640));
    const after=700+execs.length*640+350;
    setTimeout(()=>vg.classList.add('done'),after);
    setTimeout(()=>mem&&mem.classList.add('pulse'),after+650);
    setTimeout(run,after+3200);
  }
  run();
})();
</script>
</Layout>
